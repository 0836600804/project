@model IEnumerable<WebApplication1.Models.LOAIXE>

@{
    ViewBag.Title = "IndexLoaiXe";
    Layout = "~/Areas/admin/Views/Shared/user-admin.cshtml";
}

<h2>Chỉnh sửa thông tin loại xe</h2>

<p>
    @Html.ActionLink("Create New", "CreateLX")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TENLOAIXE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TENLOAIXE)
        </td>
        <td>
            @Html.ActionLink("Cập Nhật", "EditLX", new { id=item.MALOAIXE.Trim() }) |
            @Html.ActionLink("Xem chi tiết", "DetailsLX", new { id = item.MALOAIXE.Trim() }) |
            @Html.ActionLink("Xóa", "DeleteLX", new { id=item.MALOAIXE.Trim() })
        </td>
    </tr>
}

</table>
